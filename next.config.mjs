"use client";

/** @type {import('next').NextConfig} */
const nextConfig = {images: {
        domains: ['upload.wikimedia.org', 'lumiere-a.akamaihd.net', 'upload.wikimedia.org']
}
    
};

export default nextConfig;
